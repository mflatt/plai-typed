#lang setup/infotab

(define collection 'multi)

(define deps '("base"
               "plai"))
(define build-deps '("racket-doc"
                     "rackunit-lib"
                     "scribble-lib"))

(define version "1.1")
